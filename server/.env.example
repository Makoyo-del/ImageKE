# ─── ImageKE Backend (.env) ────────────────────────────────────────────────────
# Copy this file to .env and fill in real values.
# NEVER commit .env to git.

# Your Paystack SECRET key (starts with sk_test_ or sk_live_)
# Found at: https://dashboard.paystack.com/#/settings/developers
PAYSTACK_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Comma-separated list of allowed frontend origins (no trailing slash)
# Local:      http://localhost:5173
# Production: https://imageke.com,https://imageke-frontend.onrender.com
ALLOWED_ORIGINS=http://localhost:5173

# Port (Render sets this automatically — do not change for production)
PORT=5000

# ── Keep-Alive Ping Secret ────────────────────────────────────────────────────
# A strong random token used to authenticate cron-job calls to GET /api/ping.
# Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Set this same value as the token in your cron job (cron-job.org / UptimeRobot / GitHub Actions).
# If left unset, the /api/ping endpoint will still work but will log a warning.
PING_SECRET=your_strong_random_token_here

# ── Subscription Token Secret ──────────────────────────────────────────────────
# A secure, private key used to sign and verify customer monthly subscription tokens.
# Generate a secure key using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Keep this secret. If this key changes, all active client-side subscription tokens will be invalidated.
TOKEN_SECRET=your_secure_subscription_signing_key_here

# ─── Supabase Configuration ───────────────────────────────────────────────────
SUPABASE_URL=your_supabase_project_url
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key
SUPABASE_ANON_KEY=your_supabase_anon_key

# ─── Resend Configuration ─────────────────────────────────────────────────────
RESEND_API_KEY=your_resend_api_key
RESEND_FROM_EMAIL=alerts@duncanmakoyo.com

# ─── HookBunker Configuration ─────────────────────────────────────────────────
# The public URL of the HookBunker dashboard — used in alert/notification emails.
# Set this to your production frontend URL (including hash routing '/#/').
HOOKBUNKER_DASHBOARD_URL=https://duncanmakoyo.com/#/hookbunker/dashboard

# ─── Career Academy Configuration ────────────────────────────────────────────
# The "from" address for all academy emails sent via Resend.
# Must be a verified sender domain in your Resend account.
ACADEMY_EMAIL_FROM=duncan@duncanmakoyo.com

# The public URL of the Academy dashboard — used in welcome/alert emails.
ACADEMY_DASHBOARD_URL=https://duncanmakoyo.com/#/academy/dashboard

# The single-use WhatsApp community invite link sent to students after payment.
# Replace this with a real link from WhatsApp > Community > Invite via link.
ACADEMY_WHATSAPP_LINK=https://chat.whatsapp.com/your-community-link-here

# ─── AI Job Seeker Workshop Configuration ────────────────────────────────────
# Workshop emails use the global RESEND_API_KEY and EMAIL_FROM above.
# No separate Resend key needed — all workshop mail flows through the same account.

# The Google Meet link for the live session — included in every confirmation and
# reminder email. Do NOT change this once registrants have received their emails.
WORKSHOP_MEETING_LINK=https://meet.google.com/gof-rfcr-hno

# The WhatsApp group invite link for the workshop cohort.
# Sent inside confirmation emails. Replace if the link expires.
WORKSHOP_WHATSAPP_LINK=https://chat.whatsapp.com/HhehXfi5reR4RzXOHh3rdo

# Workshop date and time shown in confirmation and reminder emails.
# Format: human-readable string. Update if the schedule changes.
WORKSHOP_SESSION_DATE=Saturday, 18th July 2026
WORKSHOP_SESSION_TIME=2:00 PM EAT
